machine:
  services:
    - docker

test:
  override:
    - npm run start-mockapi:
        background: true
    - npm run lint
    - npm run test:wait-for-mock

deployment:
  testing:
    branch: testing
    commands:
      - ./deploy testing
